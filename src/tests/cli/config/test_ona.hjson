#hjson
// * 🎯用于测试ONA
// ! ⚠️使用了与项目无关的本地路径
//  * 📝ONA依赖cygwin，故即便内置也容易出错
{
    // 转译器支持单独指定「输入转译器」和「输出转译器」
    translators: {
        in: ona
        out: ona
    }
    command: {
        cmd: ../../NARS-executables/NAR.exe
        cmdArgs: [
            shell
        ]
    }
    autoRestart: true
}