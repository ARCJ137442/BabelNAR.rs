#hjson
// * 用于测试「预加载NAL输入」，加载「操作测试」
{
    preludeNAL: {
        // 预置的NAL测试文件
        file: ./src/tests/nal/test_operation.nal
    }
    // 禁止用户输入
    userInput: false
    // 不自动重启
    autoRestart: false
    // 开启严格模式
    // * 🎯用于自动化测试中捕获错误
    //   * ✨可由此被外部脚本调用
    strictMode: true
}